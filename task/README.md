# `task/` — agent scratch output (not committed)

Working directory for the MessageSpring **ms-forge-eco** agents (e.g. the
`/f-po`, `/f-plan`, `/f-brainstorm` flows) to drop temporary artifacts —
requirements, design references, plans, notes.

Everything in this folder is **gitignored** except this README (see
`.gitignore`). Don't commit its contents; treat them as disposable working
files.
