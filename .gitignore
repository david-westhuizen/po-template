node_modules
dist
dist-ssr
*.local
.env
.env.*
!.env.example

# editor
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store

# logs
logs
*.log
npm-debug.log*

# Claude Code local settings — may hold secrets (e.g. the Supabase MCP token)
.claude/settings.local.json

# Vercel CLI link metadata (created by `vercel link`)
.vercel

# ms-forge-eco agent scratch output — temp files, never commit
/task/*
!/task/README.md

# Terraform (managed by ms-infra-github)
.terraform/
*.tfstate
*.tfstate.*
*.tfvars
!*.tfvars.example
crash.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json
.terraformrc
terraform.rc
